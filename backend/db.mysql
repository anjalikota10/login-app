<?php
$servername = "mysql"; // This will be the service name in Docker Compose
$username = "root";
$password = "root_password"; // Set the same password as in Docker Compose
$dbname = "login_db";

// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>

